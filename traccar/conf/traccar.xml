<?xml version='1.0' encoding='UTF-8'?>

<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>

<properties>

    <!-- Web interface -->
    <entry key='web.origin'>*</entry>
    <entry key='web.registration'>true</entry>

    <!-- Database: built-in H2 for Phase 1 testing (swap to PostgreSQL in Phase 3) -->
    <entry key='database.driver'>org.h2.Driver</entry>
    <entry key='database.url'>jdbc:h2:/opt/traccar/data/database</entry>
    <entry key='database.user'>sa</entry>
    <entry key='database.password'></entry>

    <!-- OsmAnd protocol — used for GPS simulation -->
    <entry key='osmand.port'>5055</entry>

    <!-- Mictrack MP91 OBD-II devices — protocol is H02 or Huabao, both map to port 5013 -->
    <!-- If device does not report after first connect, swap huabao for h02 in the entry below -->
    <entry key='huabao.port'>5013</entry>
    <!-- <entry key='h02.port'>5013</entry> -->

    <!-- Filter: ignore positions with speed > 200 km/h (removes noise) -->
    <entry key='filter.enable'>true</entry>
    <entry key='filter.maxSpeed'>200</entry>

    <!-- Event processing -->
    <entry key='event.enable'>true</entry>
    <entry key='processing.computedAttributes.enable'>true</entry>

</properties>
